class Heap{
    int [] A; 
    int size ; 

    public Heap(int n){
        A = new  int[n];
        size = n ; 
    }
}
class g1{

    // balenced BST have a built TC -- O(NlogN)
    // Heap -- data stru simpliar to BST

    // Complete BT  -- all lvl have to be completely filled except possibly the last lvl (left to right)
    
    // Heap --- arr arranged in a heap order 
    // root node should have the smallest value and should be a complete BT
    // left and right child of index ith -- (2i + 1) (2i + 2)
    // for indx  -- floor division of (i-1)/2

    





}